"""Configuration loading and validation entry points.

Step 3 will turn this module into the typed loader for `config/x.json`. It is
kept as a separate module now so CLI and application services do not parse
configuration directly.
"""
